global:
  resolve_timeout: 5m

route:
  receiver: operations
  group_by: [alertname, severity, application]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: operations
    webhook_configs:
      - url: '__ALERTMANAGER_WEBHOOK_URL__'
        send_resolved: true
