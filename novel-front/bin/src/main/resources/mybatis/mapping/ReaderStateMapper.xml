<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java2nb.novel.mapper.ReaderStateMapper">

    <resultMap id="progress" type="com.java2nb.novel.service.reader.ReaderProgressRow">
        <id column="user_id" property="userId"/>
        <result column="book_id" property="bookId"/>
        <result column="book_index_id" property="bookIndexId"/>
        <result column="paragraph_index" property="paragraphIndex"/>
        <result column="character_offset" property="characterOffset"/>
        <result column="progress_percent" property="progressPercent"/>
        <result column="version" property="version"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="annotation" type="com.java2nb.novel.service.reader.ReaderAnnotationRow">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="book_id" property="bookId"/>
        <result column="book_index_id" property="bookIndexId"/>
        <result column="annotation_type" property="type"/>
        <result column="paragraph_index" property="paragraphIndex"/>
        <result column="character_offset" property="characterOffset"/>
        <result column="selected_text" property="selectedText"/>
        <result column="note_text" property="noteText"/>
        <result column="version" property="version"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="progressColumns">
        user_id, book_id, book_index_id, paragraph_index, character_offset,
        progress_percent, version, create_time, update_time
    </sql>

    <sql id="annotationColumns">
        id, user_id, book_id, book_index_id, annotation_type, paragraph_index,
        character_offset, selected_text, note_text, version, create_time, update_time
    </sql>

    <select id="selectProgress" resultMap="progress">
        SELECT <include refid="progressColumns"/>
        FROM reader_progress
        WHERE user_id = #{userId} AND book_id = #{bookId}
        LIMIT 1
    </select>

    <insert id="upsertProgress">
        INSERT INTO reader_progress
            (user_id, book_id, book_index_id, paragraph_index, character_offset, progress_percent, version)
        VALUES
            (#{userId}, #{bookId}, #{bookIndexId}, #{paragraphIndex}, #{characterOffset}, #{progressPercent}, 0)
        ON DUPLICATE KEY UPDATE
            book_index_id = VALUES(book_index_id),
            paragraph_index = VALUES(paragraph_index),
            character_offset = VALUES(character_offset),
            progress_percent = VALUES(progress_percent),
            version = version + 1,
            update_time = CURRENT_TIMESTAMP(3)
    </insert>

    <select id="listAnnotations" resultMap="annotation">
        SELECT <include refid="annotationColumns"/>
        FROM reader_annotation
        WHERE user_id = #{userId} AND book_id = #{bookId} AND book_index_id = #{bookIndexId}
        ORDER BY character_offset, id
    </select>

    <select id="selectAnnotationOwned" resultMap="annotation">
        SELECT <include refid="annotationColumns"/>
        FROM reader_annotation
        WHERE id = #{annotationId} AND user_id = #{userId}
        LIMIT 1
    </select>

    <insert id="insertAnnotation" parameterType="com.java2nb.novel.service.reader.ReaderAnnotationRow"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO reader_annotation
            (user_id, book_id, book_index_id, annotation_type, paragraph_index,
             character_offset, selected_text, note_text, version)
        VALUES
            (#{userId}, #{bookId}, #{bookIndexId}, #{type}, #{paragraphIndex},
             #{characterOffset}, #{selectedText}, #{noteText}, 0)
    </insert>

    <update id="updateAnnotationOwned">
        UPDATE reader_annotation
        SET note_text = #{noteText}, version = version + 1
        WHERE id = #{annotationId} AND user_id = #{userId} AND version = #{expectedVersion}
    </update>

    <delete id="deleteAnnotationOwned">
        DELETE FROM reader_annotation
        WHERE id = #{annotationId} AND user_id = #{userId} AND version = #{expectedVersion}
    </delete>

</mapper>
